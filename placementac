local ReplicatedStorage = game:GetService("ReplicatedStorage")
local selection = _G.Selection or {}
while true do
    local request_makima_sacrifice = ReplicatedStorage.endpoints.client_to_server.request_makima_sacrifice

    request_makima_sacrifice:InvokeServer(unpack(selection))
    wait(10)
end
