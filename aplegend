local ReplicatedStorage = game:GetService("ReplicatedStorage")

if not game:IsLoaded() then
    game.Loaded:Wait()
end

local legend = {
    [1] = "Create",
    [2] = "Hueco_Mundo",
    [3] = "Legend",
    [4] = "3",
    [5] = false,
    [6] = "Normal"
}

local start = {
    [1] = "Start",
}

local legendFolder = ReplicatedStorage:WaitForChild("Remotes")
local mapFolder = legendFolder:WaitForChild("Pod")


pcall(function()
    mapFolder:FireServer(unpack(legend))
    task.wait(0.5)
    mapFolder:FireServer(unpack(start))
end)
